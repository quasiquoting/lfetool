(defmodule unit-{{PROJECT}}-tests
  (behaviour ltest-unit)
  (export all))

(include-lib "ltest/include/ltest-macros.lfe")

(deftest code-change
  (is-equal
    ;; XXX This unit test fails by default -- fix it!
    #(ok "data")
    ({{PROJECT}}-server:code_change
      "old version"
      "state"
      "extra")))
